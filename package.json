{
  "name": "claude-code-wechat-channel",
  "version": "0.1.0",
  "description": "Claude Code Channel plugin for WeChat — bridge WeChat messages into Claude Code sessions via the official ilink API",
  "type": "module",
  "private": true,
  "scripts": {
    "setup": "bun setup.ts",
    "start": "bun wechat-channel.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "license": "MIT"
}
